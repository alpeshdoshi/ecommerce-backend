package com.educative.ecommerce.config;

//a class to store the string for reusability.
// if we want to change an error message, we just need to change it in one place
public class MessageStrings {

    public static final String AUTH_TOEKN_NOT_PRESENT = "authentication token not present";
    public static final String AUTH_TOEKN_NOT_VALID = "authentication token not valid";
    public static final String WRONG_PASSWORD = "please check the password";
}